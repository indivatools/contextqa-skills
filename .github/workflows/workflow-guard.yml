# Per-repo stub. Logic lives in indivatools/.github. Do not edit here.
name: workflow-guard
on:
  pull_request:
  pull_request_review:
jobs:
  workflow-guard:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: indivatools/.github/.github/actions/workflow-guard@main
        with:
          github-token: ${{ github.token }}
